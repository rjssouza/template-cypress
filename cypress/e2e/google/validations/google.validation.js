import validation from '../../../validations/general.validation';
import selector from '../page/google.selector';

const assertGoogleTitle = () => {
  cy.expect(true).to.be.true;
};

export default { assertGoogleTitle };
